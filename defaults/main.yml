ros2_distribution: galactic
ros2_user.name: '{{ ansible_user }}'
ros2_user.group: '{{ ansible_user }}'

ros2_workspace: '/home/{{ ros2_user }}/{{ ros2_distribution }}_ws/'


# launch files can be installed as systemd services
# 
launch_service_env_dir: /etc/ros2_systemd